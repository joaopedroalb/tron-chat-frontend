import styled from "styled-components";

export const ChatBox = styled.div`
    width: 80%;
    min-height: 90vh;
    border-radius: 20px;
    padding: 2rem;
    background: var(--gray);
    color: var(--white);

    display: flex;
    flex-direction: column;

`